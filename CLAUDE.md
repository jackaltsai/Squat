# 深蹲智慧健身輔助系統 — 開發規劃書

> 依據論文《基於影像姿態辨識之深蹲智慧健身輔助系統設計與評估框架》規劃之 Android 原型實作路線圖。
> 本文件供 Claude Code 於 Android Studio 中作為開發依循基準，建議放在專案根目錄，命名為 `CLAUDE.md` 或 `PROJECT_PLAN.md`。

---

## 0. 專案概述

### 0.1 目標
在 Android 手機端，以單一前視角相機即時偵測深蹲動作，提供：
- 穩定的深蹲計次（五階段狀態機）
- 個人化基準深度校正與深度達成率計算
- 三段式（綠/黃/紅）分級回饋（視覺 + 語音）
- 膝內夾判定
- 訓練紀錄儲存，為後續量化驗證預留資料格式

### 0.2 系統架構（四層）

```
感知層           CameraX 即時影像串流
     ↓
數據處理層        ML Kit Pose Detection → 關鍵點擷取
                 品質檢查（信心值過濾）+ EMA 平滑處理
     ↓
核心邏輯層        五階段狀態機（計次 + 最低點判定）
                 個人化深度校正模組（Duser、深度達成率 p）
                 姿勢判定模組（膝內夾、深度分級）
     ↓
輸出應用層        UI 視覺回饋（綠/黃/紅）、語音提示、訓練紀錄儲存
```

### 0.3 技術棧
| 項目 | 選用技術 |
|---|---|
| 語言 | Kotlin |
| UI | Jetpack Compose |
| 相機 | CameraX (`camera-camera2`, `camera-lifecycle`, `camera-view`) |
| 姿態辨識 | Google ML Kit Pose Detection（`pose-detection` 或 `pose-detection-accurate`） |
| 本地儲存 | Room |
| 語音提示 | Android `TextToSpeech` |
| 最低 SDK | minSdk 24（ML Kit Pose Detection 需求），建議 targetSdk 最新穩定版 |

### 0.4 核心設計參數（對應論文 3.4 / 3.5 / 3.6）
- 五階段狀態機：`STAND → DOWN → BOTTOM → UP → (回到 STAND 計次+1)`
- 最低點判定依據：**下降轉上升的轉折點**，而非單一高度閾值
- 個人化基準深度 `Duser`：站姿校正（約 3 秒穩定）+ 兩次基準深蹲取平均
- 深度達成率：`p = Dnow / Duser`
- 三段式回饋門檻（可調整，初始值採論文表 1 範例）：

| 模式 | 綠色 | 黃色 | 紅色 |
|---|---|---|---|
| 入門 L1 | p ≥ 0.90 | 0.90 > p ≥ 0.80 | p < 0.80 |
| 一般 L2 | p ≥ 1.00 | 1.00 > p ≥ 0.85 | p < 0.85 |
| 進階 L3 | p ≥ 1.10 | 1.10 > p ≥ 0.95 | p < 0.95 |

- 姿勢判斷（膝內夾 + 深度分級）僅在 `BOTTOM` 觸發一次，避免畫面閃爍與提示過於頻繁

---

## 1. 里程碑總覽

| 里程碑 | 名稱 | 核心產出 | 對應論文章節 |
|---|---|---|---|
| M1 | 相機 + 骨架顯示 | 可即時顯示髖膝踝關鍵點的 Demo | 3.2 / 3.3 |
| M2 | 品質控管 + 計次狀態機 | 自動深蹲計次功能可用 | 3.3 / 3.4 |
| M3 | 個人化校正 + 分級回饋 | 校正流程 + 三段式顏色回饋 UI | 3.5 |
| M4 | 姿勢判定 + 訓練紀錄 | 膝內夾判定、語音提示、資料落地 | 3.6 / 4.1 / 4.2 |

每個里程碑結束時應能在實機上跑出可操作、可觀察結果的版本，不追求一次到位。

---

## 2. M1｜相機 + 骨架顯示

### 目標
確認 CameraX 影像串流正常，ML Kit 能穩定取得髖、膝、踝關鍵點，並在畫面上疊圖顯示。

### 任務清單
- [ ] 建立 Android Studio 專案（Empty Compose Activity）
- [ ] 設定 `build.gradle` 依賴：CameraX、ML Kit Pose Detection
- [ ] 申請並處理 `CAMERA` 權限（Compose + Accompanist 或原生 `ActivityResultContracts`）
- [ ] 實作 `CameraPreview`（`PreviewView` 包在 `AndroidView` 中）
- [ ] 建立 `ImageAnalysis` use case，串接 `PoseDetector`（`STREAM_MODE`）
- [ ] 取出 `LEFT_HIP / RIGHT_HIP / LEFT_KNEE / RIGHT_KNEE / LEFT_ANKLE / RIGHT_ANKLE` 六個關鍵點
- [ ] 用 Compose `Canvas` 在預覽畫面上疊圖畫出關鍵點與骨架連線
- [ ] 畫面上同時顯示各關鍵點的 `inFrameLikelihood`（信心值），方便肉眼判斷抓取穩定度
- [ ] 實機測試：不同距離（1.5m / 2m / 2.5m）、不同光線、不同背景下的抓取穩定度記錄

### 驗收標準
- 手機直立、使用者全身入鏡時，六個下肢關鍵點能連續穩定顯示，無明顯掉幀或跳動
- 確認前鏡頭與後鏡頭的座標系差異（是否需要鏡像處理）

### 給 Claude Code 的提示
> 「幫我在這個 Android Studio 專案裡建立 M1 階段：CameraX + ML Kit Pose Detection 的即時骨架顯示 Demo，只需要疊圖顯示髖膝踝六個關鍵點與信心值，先不做狀態機邏輯。」

---

## 3. M2｜品質控管 + 計次狀態機

### 目標
在 M1 的關鍵點資料流上，加入品質過濾與平滑處理，並實作五階段狀態機完成自動深蹲計次。

### 任務清單
- [ ] 實作信心值過濾：任一必要關鍵點 `inFrameLikelihood` 低於門檻（初始建議 0.6）則整幀捨棄
- [ ] 實作 EMA（指數移動平均）平滑器，對髖/膝/踝座標做平滑（alpha 初始建議 0.2~0.4，之後需實測調整）
- [ ] 定義 `SquatState` enum：`STAND / DOWN / BOTTOM / UP`
- [ ] 實作狀態機轉換邏輯：
  - `STAND → DOWN`：髖部高度相對站立基準明顯下降
  - `DOWN → BOTTOM`：偵測到髖部運動方向由下降轉為上升，且轉折持續若干連續幀（避免瞬間閾值誤判）
  - `BOTTOM → UP`：判定最低點後立即進入
  - `UP → STAND`：重新回到接近站立基準高度並穩定若干幀後，**計次 +1**
- [ ] 在畫面上即時顯示目前狀態（STAND/DOWN/BOTTOM/UP）與累計次數
- [ ] 記錄除錯用 log（每幀髖部 Y 座標、EMA 後座標、目前狀態），方便後續調參
- [ ] 實機測試：正常深蹲、刻意晃動不深蹲、半蹲等情境下計次是否正確

### 驗收標準
- 連續 10 下標準深蹲，系統計次誤差在可接受範圍內（先求邏輯正確，精確度後續於 M4/資料蒐集階段量化）
- 站立時輕微晃動不會被誤判為一次深蹲

### 給 Claude Code 的提示
> 「延續 M1 的專案，幫我加入 EMA 平滑處理與信心值過濾，並實作一個五階段狀態機（STAND/DOWN/BOTTOM/UP）做深蹲自動計次，最低點判定要用『下降轉上升的轉折點加上連續幀確認』，不要用單一高度閾值。」

---

## 4. M3｜個人化校正 + 分級回饋

### 目標
加入使用者個人化基準深度校正流程，並依訓練模式提供三段式（綠/黃/紅）視覺回饋。

### 任務清單
- [ ] 實作「站姿校正」流程：要求使用者站立維持約 3 秒，取這段時間髖部 Y 座標平均值作為站立基準 `standBaselineY`
- [ ] 實作「基準深蹲校正」流程：引導使用者完成 2 次舒服、可控制的深蹲，取兩次最低點深度平均作為 `Duser`
- [ ] 校正流程需要清楚的 UI 引導（倒數計時、文字提示、完成確認）
- [ ] 實作深度達成率計算：每次 `BOTTOM` 觸發時計算 `Dnow`，並求 `p = Dnow / Duser`
- [ ] 建立 `TrainingMode` enum（入門 L1 / 一般 L2 / 進階 L3），對應論文表 1 門檻值
- [ ] 實作模式選擇 UI（訓練前選擇入門/一般/進階）
- [ ] 依 `p` 值與所選模式，顯示綠/黃/紅三色回饋（只在 BOTTOM 觸發一次，避免閃爍）
- [ ] 回饋 UI 設計建議：全螢幕邊框變色 或 明顯色塊 + 文字（例如「深度達標！」「再蹲深一點」）

### 驗收標準
- 校正流程順暢，使用者能理解操作步驟
- 同一使用者切換不同模式（入門/一般/進階），同樣深度會得到不同分級結果，符合表 1 邏輯

### 給 Claude Code 的提示
> 「延續 M2，幫我加入站姿校正與基準深蹲校正流程，計算個人化基準深度 Duser，並依照論文表 1 的三段式門檻（入門/一般/進階模式），在每次 BOTTOM 觸發時顯示綠/黃/紅回饋。」

---

## 5. M4｜姿勢判定 + 訓練紀錄

### 目標
加入膝內夾判定、語音提示，並將每次深蹲的分析結果落地儲存，為後續資料蒐集與量化驗證做準備。

### 任務清單
- [ ] 實作膝內夾判定邏輯：以髖寬正規化膝蓋相對腳踝的水平位移，設定門檻判斷是否內夾（初始門檻需後續用驗證集調整，見第 6 節）
- [ ] 膝內夾判定同樣僅在 `BOTTOM` 觸發一次
- [ ] 整合 `TextToSpeech`，依回饋結果播放對應語音提示（例如：紅色→「蹲深一點」；膝內夾→「膝蓋往外一點」）
- [ ] 設計 Room Entity 儲存每次深蹲紀錄，至少包含：
  - 時間戳記
  - 深度達成率 `p`
  - 是否膝內夾
  - 回饋顏色（綠/黃/紅）
  - 當次訓練模式
- [ ] 實作訓練歷程頁面（Compose），顯示當次訓練的次數、達標比例、膝內夾比例
- [ ] 加入「研究/除錯模式」開關：開啟後將每幀原始關鍵點座標、EMA 後座標、狀態機狀態輸出為 CSV/JSON，供後續人工標註比對使用

### 驗收標準
- 完整跑完一組訓練（例如 10 下），能在歷程頁面看到正確統計
- 除錯模式輸出的 CSV/JSON 格式清楚、可直接匯入 Python 分析

### 給 Claude Code 的提示
> 「延續 M3，幫我加入膝內夾判定（用髖寬正規化膝踝水平位移）、語音提示（TextToSpeech），並用 Room 儲存每次深蹲的分析結果，另外加一個除錯模式可以把每幀原始資料輸出成 CSV。」

---

## 6. M4 之後：參數優化與資料蒐集（對應論文 3.7 / 4.1 / 4.2）

M1~M4 完成後即進入原型驗證階段，非 App 開發範疇，但建議提前規劃：

- **驗證集試門檻方法**：蒐集深蹲影片並建立人工標註（計次、膝內夾、深度等級），對候選門檻值逐一比對系統輸出與人工標註的一致程度，選出最佳參數組合
- **受試者與資料蒐集**：預計約 30 名志工，每人 2 組 × 10 下，統一拍攝距離/高度/光照/背景規範
- **評估指標**：
  1. 計次誤差（平均誤差、誤差分布）
  2. 膝內夾判斷一致率（Accuracy / F1-score）
  3. 深度分級一致率（Accuracy）
  4. 門檻優化前後比較
- **倫理與隱私**：取得受試者同意、資料匿名化、優先儲存骨架關鍵點與判定結果而非原始人臉影像

---

## 7. 開發時的注意事項

- 論文的最低點判定核心是「下降轉上升的轉折 + 連續幀確認」，**不要**簡化成單一高度閾值判斷，這是系統穩定性的關鍵設計
- 所有姿勢判斷（膝內夾、深度分級）都應該只在 `BOTTOM` 觸發一次，這是為了避免論文提到的「提示頻繁閃爍」問題
- EMA alpha、信心值門檻、狀態機的 `stableFrames`、膝內夾判定門檻，這些都是需要**實測調參**的數值，不是一次寫死就好，建議在程式碼中集中放在一個 `Config` 物件方便調整
- 開發過程建議搭配 Git 版本控制，每個里程碑完成後建立 tag（`m1-camera-pose`、`m2-state-machine` 等），方便回溯與比較

---

## 8. 使用方式（給 Claude Code）

在 Android Studio 中打開 Claude Code 面板後，可以逐里程碑對話推進，例如：

```
先讀取這份 PROJECT_PLAN.md，理解整體架構後，
從 M1 開始幫我建立專案骨架。
```

每完成一個里程碑，再輸入：

```
M1 已完成並測試過，請依照 PROJECT_PLAN.md 的 M2 段落繼續開發。
```

建議每個里程碑都在實機上跑過、確認行為符合驗收標準後，再進到下一階段，避免狀態機或校正邏輯的錯誤累積到後面難以除錯。
